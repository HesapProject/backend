package uz.hesap.service.main.domain.enums;

// Staff so'rovi holati: PENDING — yuborildi, kutmoqda; ACCEPTED — qabul qilindi (xodim
// bo'ldi); REJECTED — rad etildi.
public enum StaffStatus {
  PENDING,
  ACCEPTED,
  REJECTED
}
