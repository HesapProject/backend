<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
        xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
                      http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-4.9.xsd">

    <!-- Admin foydalanuvchilar ro'yxatida "oxirgi tashrif" filtri/ustuni uchun:
         MAX(session.timestamp) per user. user_id bo'yicha indeks. -->
    <changeSet id="20260614-00-index-session-user-id" author="qodiriy">
        <sql>
            CREATE INDEX IF NOT EXISTS idx_session_user_id ON "user".session (user_id);
        </sql>
    </changeSet>

</databaseChangeLog>
