<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
        xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
                      http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-4.9.xsd">

    <!-- Rouming (Factura Provider — elektron schyot-faktura/ЭСФ) credential'lari —
         singleton (Basic auth: login+parol + baseUrl). turanix_setting bilan o'xshash. -->
    <changeSet id="20260724-00-create-rouming-setting-table" author="qodiriy">
        <sql>
            CREATE TABLE IF NOT EXISTS integration.rouming_setting (
                id                 UUID PRIMARY KEY DEFAULT uuid_generate_v4() NOT NULL,
                base_url           TEXT NOT NULL,
                login              TEXT NOT NULL,
                password           TEXT,
                created_date       TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP,
                last_modified_date TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP
            );
        </sql>
    </changeSet>

</databaseChangeLog>
